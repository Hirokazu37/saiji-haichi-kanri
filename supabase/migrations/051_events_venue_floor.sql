-- ============================================================
-- 催事に「会場（階・売場）」を追加
-- ============================================================
-- DMはがきの会場行（例: 8階 大催事場 / 地下1階 催事場）に使う。
-- 階は催事ごとに変わりうるため催事側で持つ（営業時間・最終日閉場は
-- 既存の last_day_closing_time / 百貨店テンプレート側で管理）。
-- ============================================================

ALTER TABLE events ADD COLUMN IF NOT EXISTS venue_floor TEXT;
