-- ============================================================
-- DMはがき文面の縦位置（上/中央/下）
-- ============================================================
-- 百貨店によって案内を上に置いたり下（宛名の下）に置いたりするため、
-- カード内の縦位置を保持する。値: 'top' | 'center' | 'bottom'
-- ============================================================

ALTER TABLE event_postcards ADD COLUMN IF NOT EXISTS vpos TEXT;
